# Working Tick List

Leave every item unchecked until @Otisigma confirms satisfaction with the delivered work.
Only change `[ ]` to `[x]` after that approval, and only once for the approved item.

- [ ] Dreamweaver homepage preview either plays the current public mix or clearly shows that playback is unavailable.
- [ ] The `/halo` menu cards can be collapsed and the full menu can be scrolled so the bottom cards are reachable.
- [ ] The Dreamweaver navigation path still opens `/dreamweaver/` and the `/halo` homepage experience still works.
- [ ] User approval received to tick the completed work.
